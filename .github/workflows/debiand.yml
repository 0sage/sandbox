name: debiand

on:
  push:
    branches:
      - main
    paths:
      - 'debiand/**'
  workflow_dispatch:

jobs:
  build:
    runs-on: self-hosted
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          context: debiand
          push: true
          tags: |
            ghcr.io/0sage/debiand:latest
            ghcr.io/0sage/debiand:${{ github.sha }}
